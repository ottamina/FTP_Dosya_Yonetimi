{
  "name": "ftp-manager-e2e-agents",
  "private": true,
  "type": "module",
  "scripts": {
    "plan": "node scripts/plan.mjs",
    "audit": "node scripts/audit-coverage.mjs",
    "test": "playwright test",
    "test:rename": "playwright test --grep @rename",
    "test:explorer": "playwright test --grep @explorer",
    "test:smoke": "playwright test --grep @smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
